<!-- SPECKIT START -->

For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:

- specs/014-phase11-statistics/plan.md
<!-- SPECKIT END -->

## Folder Layout (Phase 0)

```
routes/                  /api/v1/* HTTP shells (no business logic)
controllers/             one per route file; orchestrate, never query the DB directly
services/                pure business logic (program generator, calculators)
services/dataAccess/     ONLY directory allowed to import @supabase/supabase-js
services/photoStorage/   put/get/delete/url adapter — filesystem default
middleware/              requestId → requestLogger → auth → errorHandler
config/                  zod schema · dotenv adapter · loadConfig()
supabase/migrations/     forward-only timestamped SQL; each ships its RLS
seed/                    athlete + JSON catalogues + idempotent runSeed.js
frontend/                React + Vite + Tailwind workspace member
tests/{unit,integration,contract,frontend}/
```

## Key Handling Rules

- `SUPABASE_SECRET_KEY` (`sb_secret_…`) **server-only**. Imported solely by `services/dataAccess/supabaseClient.js`.
- `SUPABASE_PUBLISHABLE_KEY` (`sb_publishable_…`) is the only key that may cross to the frontend (used by future Supabase Auth flows).
- Legacy `SUPABASE_ANON_KEY` and `SUPABASE_SERVICE_ROLE_KEY` are **forbidden**; the config schema rejects them at boot with a named error.
- Secret-tagged keys (`SECRET_KEYS` in `config/schema.js`) are added to the pino redactor, so the startup log line never prints raw secrets.

## RLS Conventions

- Every domain table ships RLS in the same migration. Pattern (`research.md` §5):

  ```sql
  alter table <t> enable row level security;
  create policy <t>_select_own on <t>
    for select using (athlete_id in (select id from athletes where auth_user_id = auth.uid()));
  create policy <t>_modify_own on <t>
    for all
    using (athlete_id in (select id from athletes where auth_user_id = auth.uid()))
    with check (athlete_id in (select id from athletes where auth_user_id = auth.uid()));
  ```

- The application uses the secret-key client which bypasses RLS by design — the auth middleware is the **primary** tenant guard. RLS is defense-in-depth and exercised by `tests/integration/rls.policies.test.js` using a per-test JWT against the publishable-key client.

## Lint Rules

- ESLint at repo root (backend); frontend extends Vite's defaults.
- Prettier (`.prettierrc`) is the single formatter — width 100, single quotes, trailing commas.
- A future custom lint rule should forbid `import '@supabase/supabase-js'` outside `services/dataAccess/` and `frontend/src/lib/`.

## Test Scaffolding

- `tests/unit/` — pure Node, no network. Includes the program-generator TDD spec.
- `tests/integration/` — boots `buildApp({ config, supabase })` against the local Supabase stack (skipped automatically when `.env` is missing or Supabase is unreachable).
- `tests/contract/` — drives every path in `specs/001-phase0-foundation/contracts/openapi.yaml` via Supertest.
- `tests/frontend/` — jsdom + React Testing Library, runs through `frontend/vitest.config.js` so Vite plugins resolve correctly.

## Constitution

`./.specify/memory/constitution.md` v1.1.1. Non-negotiables: tenant-ready data model, layered architecture (no Supabase imports outside the data-access layer), config over hardcoding, versioned API (`/api/v1/`), test-first for domain logic, athlete-first UX.

## Phase 1 — Calculators Engine (added 2026-05-08)

- **Engine boundary**: every pure calculator lives under `services/engine/`. No `@supabase/supabase-js` imports in this directory or in `services/programGenerator.js` / `services/progressionEngine.js`.
- **Engine version pinning**: `services/engine/constants.js` exports `ENGINE_VERSION` (semver) and a frozen `DEFAULTS` map. Every persisted calculation row snapshots `engine_version` + `resolved_constants` so replays survive default changes. Bump rules: PATCH = rounding fix, MINOR = new calculator/optional input, MAJOR = behavioural change to an existing calculator.
- **Per-athlete overrides**: stored on `app_config.engine_overrides` (JSONB, defaults `{}`). The engine reads via `resolveConstants(override)` — defaults ⊕ override, deep-frozen.
- **Audit log**: `services/engine/auditWriter.js` is the single helper that appends a row to `calculation_results`. Called from every persisted-write path (program regenerate, 1RM record, body composition + measurement save, progression eval). NOT called from `/api/v1/calculators/*` ad-hoc endpoints (FR-029).
- **Soft-archive shapes**: `generated_programs` and `progression_flags` use `is_active` + `superseded_at`. Partial-unique indexes enforce "at most one active per scope" at the DB level.
- **Frontend routing**: `react-router-dom@^6` is wired in `frontend/src/App.jsx`. `/`, `/nutrition`, `/calculators`, and `/calculators/<slug>` are the Phase 1 routes. Phase 4's journal screen extends this.
- **Body measurement extension**: Phase 0's `body_measurements` table didn't include `neck_cm` / `hip_cm`. Phase 1 added migration `20260507000008_extend_body_measurements_neck_hip.sql` to support the U.S. Navy multi-measurement body-fat formula.

## Phase 3 — Training Program & Exercise Library (added 2026-06-02)

- **Read presenter boundary**: composed view models live under `services/trainingProgram/` (`weekView.js`, `dayView.js`, `exerciseView.js`, `mediaClassifier.js`) — pure, no `@supabase/supabase-js`. Controllers read DAOs and hand plain data to these presenters.
- **Read-only session history**: `services/dataAccess/sessions.dao.js` is the first reader of `session_journal_entries` + `session_sets` (Phase 4 owns the write path). Phase 3 never writes sessions (FR-026).
- **History-derived numbers** (test-first, `services/engine/`): `exerciseHistory.js` (`heaviestCompletedSet`, `feedSet`, `lastWeightUsed`, `recentSessions`), `loadRecommendation.js`. "Last weight" = heaviest **completed** set of the most recent session; the same feed set drives the 1RM estimate (`oneRepMax().primary_estimate_kg`) so the day row and detail page agree (SC-003). Load rec reuses the `add_load` flag's snapshotted `delta_kg`.
- **Progression indicator**: read from active `progression_flags` (`scope_ref = String(exercise_id)`); `add_load`→ready_to_increase, `regression`→regressing, else stable.
- **`/program/*` endpoints** are mounted as a SECOND router at `/api/v1/program` (after the program-generator router); paths are disjoint so unmatched requests fall through. `GET /program/week|day/:dayOfWeek|exercises/:id`.
- **Alternatives**: one new table `exercise_alternatives` (migration `20260602000001`), one-directional, `CHECK` no-self + unique no-dup, FK `ON DELETE CASCADE`, RLS in-file. DAO maps `23505`→409 CONFLICT.
- **Exercise media**: reuses `exercises.media_image_url` / `media_video_url`. Uploads go through the `photoStorage` adapter (root `PHOTO_STORAGE_ROOT`), served unauthenticated at `/static/*` so `<img>`/`<video>` can load them. YouTube videos are stored as URLs and normalized to `YOUTUBE_EMBED_HOST` by `mediaClassifier.normalizeYoutubeUrl` (shared by read + write paths). Size/type bounded by `EXERCISE_MEDIA_MAX_BYTES` / `EXERCISE_MEDIA_IMAGE_TYPES` / `EXERCISE_MEDIA_VIDEO_TYPES`.
- **Frontend**: `/program`, `/program/day/:dayOfWeek`, `/program/exercises/:id` in `App.jsx`. Live Supabase-backed contract/integration tests for the alternatives surface **probe for the `exercise_alternatives` table and skip until the migration is applied**.

## Phase 4 — Session Journal (added 2026-06-02)

- **Sole owner of the session WRITE path**: `controllers/sessions.controller.js` + `routes/sessions.routes.js` mounted at `/api/v1/sessions` (registered before `/one-rep-max-records`; `/active` is declared before `/:id`). Phase 3 stays read-only over what this phase writes. The write methods live on the existing `services/dataAccess/sessions.dao.js` (Phase 3 shipped its read side); `daos.sessions` was already wired in `app.js`.
- **Pure boundary**: composed view models + helpers live under `services/sessionJournal/` (`calendar.js` = `isoDayOfWeek`/`isSameAppDay`, `currentPhase.js` = `currentTrainingPhase`, `sessionView.js`, `summaryView.js`) — pure, no `@supabase/supabase-js`. New pure engine helpers: `services/engine/{sessionTotals,personalRecords,bodySegment}.js`. `bodySegment.js` is **extracted** from the old inline derivation in `progressionFlags.controller.js` and now shared by both (research D-6).
- **Today detection** = ISO calendar weekday (`isoDayOfWeek`, Mon=1…Sun=7) matched to `weekly_plan_slots.day_of_week`, NOT a `program_start_date` offset (D-1). **Current phase** = derived from `program_start_date` + cumulative phase `weeks` (D-8); its `rest_seconds` drives the rest timer — no `is_current` column.
- **Finish is the sole engine trigger** (D-6): `POST /sessions/:id/finish` discards incomplete sets (D-7), finalizes `ended_at`/`total_volume_kg`, then runs the Phase 1 engine exactly once — `evaluateForAthlete` → `progressionFlags.supersedeAndInsert`, `oneRepMax` → `oneRepMaxRecords.insert`, and `writeAudit` (one `one_rep_max` row per record + one `progression_eval`, both `reason:'session_finish'`). A **finish-once guard** (409 `SESSION_ALREADY_FINISHED`) + idempotent supersede make this safe without DB transactions. Auto-save (`PUT /sessions/:id/sets`) **never** runs the engine (D-5).
- **Schema**: two forward-only migrations on existing tables, **no new table** — `20260602000002` adds `session_journal_entries.day_of_week` (nullable 1–7) + a partial active-session index; `20260602000003` changes `session_sets` uniqueness to `(session_id, exercise_id, set_number)` for per-exercise numbering + extra/ad-hoc sets (D-3/D-4). RLS unchanged (existing `*_own` policies key on `athlete_id`). **Live contract/integration tests probe for the `day_of_week` column and skip until the migration is applied.**
- **Frontend**: `/journal` route + "Séance" nav in `App.jsx`. State machine in `lib/useSessionJournal.js` (idle/prompt/active/summary). One-handed `QuickStepper` (±2.5 kg / ±1 rep), `SessionTimer` (anchored to server `started_at`, D-12), `RestTimer` (Web Audio beeps via `lib/restTimerAudio.js`, silent fallback). Auto-save cadence is a **frontend Vite var** `VITE_SESSION_AUTOSAVE_INTERVAL_MS` (default 30000 in `frontend/src/lib/sessionConfig.js`) — no backend config key.

## Phase 5 — Load Tracking & Progression Algorithm (added 2026-06-02)

- **Read-only analytics layer** over Phase 4 data — **0 migrations, 0 new tables, no new dependency**. Three composed read endpoints at `/api/v1/load-tracking/*` (`overview`, `exercises/:id`, `phase-comparison`), mounted after `/sessions` in `app.js`. Pure presenter boundary `services/loadTracking/` (`statusMap.js`, `overviewView.js`, `exerciseProgressView.js`, `phaseRadarView.js`); controllers read DAOs and hand plain data to presenters. Never writes, never re-runs the engine (FR-022).
- **`one_rep_max_records` is the time series** (research D-1): one row per exercise per finished session — `source_weight_kg` (= heaviest completed set = current load + all-time record via max), `primary_estimate_kg` (= e1RM, drives trend + projection). New read `oneRepMaxRecords.seriesForAthlete`. Volume/last-10 come from `sessions.dao.recentSessionVolumesForExercise` (D-11).
- **Trend + projection** (pure `services/engine/trendProjection.js`, D-2/D-3): trend = signed e1RM change over the last **30 days**, `flat` when |Δ| ≤ **1%** (fixed dead-band, NOT `on_pace_pct_per_month`); 8-week projection = **least-squares linear fit** over the e1RM series, `null` below **3** points. The detail chart's primary line is the e1RM series (the projection extends it); working load is the secondary line; all-time record is the heaviest completed set, annotated (I1).
- **Status map** (`statusMap.js`, D-5): the 5 persisted `progression_flags` types → 4 per-exercise badges (`add_load`→ready, `regression`→regressing, muscle-group `stagnation`→stagnation when the exercise has no own actionable flag, else `maintain`) + a separate muscle-group **deload notice**.
- **Phase attribution** (D-7): `phaseForDate` extracted from `currentTrainingPhase` (which now delegates to it) buckets sessions by date; the radar (`phaseRadarView.js`, D-8) = avg per-day top working load per muscle group per phase.
- **Charts are hand-rolled SVG** (D-9): pure geometry in `frontend/src/lib/chartGeometry.js` (`linearScale`/`linePath`/`barRects`/`radarPolygon`/`niceTicks`, unit-tested) drives `components/charts/{LineChart,BarChart,RadarChart}.jsx`. No charting library. Frontend route tree `/load-tracking`, `/load-tracking/exercises/:id`, `/load-tracking/phases` + "Charges" nav.

## Phase 7 — Nutrition & Calories (added 2026-06-03)

- **Write-heavy phase — 3 new tables, RLS in-migration**: `nutrition_logs` (one row per food per meal-slot per day; `food_id`→`foods` **ON DELETE RESTRICT**; persists a **macro snapshot** `kcal/protein_g/carbs_g/fat_g` so past totals never shift when a catalogue food is edited — FR-003/SC-008), `hydration_log` (per-athlete-per-day counter, PK `(athlete_id, logged_on)`), `nutrition_template_meal_items` (concrete foods+grams per slot for "Load daily plan", seeded by `runSeed.js`). All ship `*_own` policies in-file (Constitution I). `nutrition_logs` finally backs the name the Phase 2 reset DAO already reserved — `reset.dao` `FULL_WIPE_ORDER` deletes the food-referencing children **before** `foods` (RESTRICT ordering).
- **Targets are read, never recomputed** (D-3/FR-008): daily kcal/macros come from the **shared** `services/nutrition/targets.js#resolveTargets({ daos, athleteId })` (extracted from `nutritionTargets.controller`, the single source consumed by the day view + trends + the targets endpoint). Food/water logging does **not** write the `calculation_results` audit log (it is summation, not an engine calculation — FR-029).
- **Custom food persists to the catalogue** (FR-002a): the `foods` table is athlete-writable here via `foods.dao.createForAthlete` (upsert on `(athlete_id, slug, locale)` using `services/nutrition/foodSlug.slugify`, so a duplicate name reconciles — no dup). `POST /nutrition/log` accepts `food_id` **or** an inline `custom_food` (create-then-log).
- **Load daily plan is non-destructive** (FR-011, clarification): `POST /nutrition/load-plan` requires `mode` (`replace`|`append`) on a non-empty day, else `409 LOAD_PLAN_CONFLICT` — never a silent overwrite. Hydration goal resolves `app_config.engine_overrides.hydration.goal_ml ?? HYDRATION_GOAL_ML` (per-athlete setter is a Phase 2 concern).
- **Pure boundary**: `services/engine/{nutritionMath,nutritionTrends}.js` (entryMacros/dayTotals/progress; caloriesByDay/macroBreakdown/**weeklyAvgProtein = mean daily protein/week**, D-7) + `services/nutrition/{foodSlug,targets,dayView,trendsView}.js` — all pure, test-first (Constitution V), no `@supabase` import. Controllers read DAOs and hand plain data to these presenters; the future-date guard reads the clock at the controller boundary (`isoDay(now())`), never inside a pure fn.
- **Endpoints** extend the existing `/api/v1/nutrition` router: `GET /day`, `POST/PATCH/DELETE /log`, `POST /load-plan`, `POST /hydration`, `GET /trends` (+ `POST /foods` custom-food create on the foods router). Config keys: `HYDRATION_GOAL_ML` (3000), `NUTRITION_TREND_DAYS` (30), `NUTRITION_LOCALE` (`fr-FR`).
- **Charts reuse Phase 5 SVG** + two new pure `chartGeometry` helpers `gaugeArc`/`donutSegments` (unit-tested) driving `components/charts/{HydrationGauge,DonutChart}.jsx`. Frontend `/nutrition` (day log: 4 bars, 5 meal slots, search, custom food, load-plan, hydration gauge) + `/nutrition/trends` (calories-30d + goal line, macro donut, weekly protein). Live contract/integration tests probe for `nutrition_logs` and **skip until migrations are applied**.

## Phase 8 — Supplements (added 2026-06-03)

- **Write-heavy phase — 2 new tables, RLS in-migration**: `supplement_intake_log` (one row per athlete per supplement per day; **presence ≡ taken**, absence on an elapsed day ≡ missed; `UNIQUE(athlete_id, supplement_id, logged_on)` makes "mark taken" idempotent — FR-003/SC-002; `supplement_id`→`supplements` **ON DELETE CASCADE**) and `supplement_weekly_assessment` (one row per athlete per ISO week; four `CHECK (… between 1 and 5)` columns `energy/recovery/sleep_quality/strength`; `UNIQUE(athlete_id, week_start)`). Both ship `*_own` policies in-file (Constitution I). `reset.dao` clears both children **before** `supplements` (`MODULE_TABLES.supplements` + `FULL_WIPE_ORDER`).
- **Read-only over the seeded catalogue** (FR-020): the `supplements` table (Phase 0) is **not** edited here — Phase 8 records adherence against it; catalogue add/remove/rename is a Phase 2 concern. Supplement logging runs **no** calculator/progression engine and writes **no** `calculation_results` (it is adherence recording, not a calculation — mirrors the Phase 7 food/water boundary).
- **Clarified invariants baked into pure logic** (clarifications 2026-06-03): the **editable window is the current ISO week only** — the intake toggle rejects a future date (`422 FUTURE_DATE`) and any day outside the current ISO week (`422 OUTSIDE_EDIT_WINDOW`), prior weeks are read-only; **streaks anchor at `program_start_date`** (read from the athlete profile at the controller boundary, never counted earlier); **weeks are ISO weeks** (Monday start, via pure `services/supplements/week.js#isoWeekStart`, consistent with Phase 4 `isoDayOfWeek`); the **self-assessment upsert targets the server-derived current ISO week only**, so elapsed weeks are read-only.
- **Pure boundary**: `services/engine/{supplementStreaks,supplementGrid}.js` (`streakForSupplement` — today-not-taken doesn't break, gap resets, program-start floor, never-taken=0; `cellStatus` → taken/missed/upcoming, missed is retrospective-only) + `services/supplements/{week,checklistView,weekGridView,assessmentView}.js` — all pure, test-first (Constitution V), no `@supabase` import. The clock is read once at the controller boundary (`isoDay(now())`), never inside a pure fn.
- **Endpoints** extend the existing `/api/v1/supplements` router (`GET /` catalogue unchanged): `GET /checklist`, `POST /intake`, `GET /grid`, `GET /assessments`, `PUT /assessment`. The router/controller now take `({ daos, config })`; daos `supplementIntake` + `supplementAssessments` wired in `app.js`. Config keys: `SUPPLEMENT_PRIMARY_SLUG` (`creatine-monohydrate`, the prominently-shown streak) and `SUPPLEMENT_ASSESSMENT_TREND_WEEKS` (12).
- **Frontend reuses Phase 5 SVG `LineChart`** (no new chart geometry); the weekly grid is plain Tailwind markup. `/supplements` (checklist cards + one-tap toggle + per-supplement streaks with a creatine hero + weekly taken/missed/upcoming grid + current-week self-assessment form) + `/supplements/trends` (4-dimension assessment trend) + "Suppléments" nav. Live contract/integration tests probe for `supplement_intake_log` and **skip until migrations are applied**; intake/assessment integration tests snapshot-and-restore the current-week row so they never destroy real data.

## Phase 9 — Recovery & Wellbeing (added 2026-06-03)

- **Schema-light — 0 new tables, 1 migration, RLS unchanged**: `20260603000006_extend_recovery_log_phase9.sql` is a pure `ALTER TABLE … ADD COLUMN` on the Phase 0 `recovery_log` (already `athlete_id`-scoped, `UNIQUE(athlete_id, logged_on)`, ships `recovery_log_select_own`/`recovery_log_modify_own`) adding `sleep_quality int CHECK (1–5)`, `mood text`, and `sore_zones text[] NOT NULL DEFAULT '{}'` — the new columns inherit the existing row policies (no new policy, no new RLS test). The legacy scalar `soreness int (0–10)` stays but is **deprecated/superseded** by the zone set (new writes don't set it, reads ignore it — clarification D-1); energy/stress reuse the existing **0–10** columns (no re-scale of seeded data, D-3). `reset.dao` is unchanged (`recovery_log` is already a reset module + in `FULL_WIPE_ORDER`; the array column drops with the row).
- **Recovery logging is journaling — no engine, no audit** (FR-020, D-5): check-in writes call **only** `recovery.dao.upsert` and write **no** `calculation_results`; they never invoke the calculators, progression engine, `programGenerator`, or `auditWriter` (mirrors the Phase 7 food/water and Phase 8 supplement boundaries). The session journal (Phase 4) stays **read-only** here — one thin new `sessions.dao.dailyTrainingVolumes(athleteId,{from,to})` returns finished sessions (`ended_at IS NOT NULL`) as `[{ ended_at, total_volume_kg }]` for the scatter's performance axis (FR-018, D-7); Phase 9 never writes session data.
- **Clarified invariants baked into pure logic** (clarifications 2026-06-03): the **editable window is the current ISO week only** — `PUT /recovery/checkin` rejects a future `logged_on` (`422 FUTURE_DATE`) and any day outside the current ISO week (`422 OUTSIDE_EDIT_WINDOW`), prior weeks stay viewable (GET works for any past date) but immutable; the guard **reuses the pure Phase 8 `services/supplements/week.js`** (`isoWeekStart`/`isCurrentIsoWeek` — generic, no supplement coupling, D-2/D-6). **Soreness = a set of zones with no per-zone intensity** (`sore_zones`, empty set = explicit "no soreness reported", distinct from "no check-in" = no row); **partial save** sets only the columns the controller validated as present and never substitutes a value for an omitted field (FR-003); **day grain is the server UTC calendar day** (`date` query defaults to today UTC).
- **Pure boundary**: `services/engine/{recoveryAlerts,recoveryTrends}.js` (`evaluateAlerts(recentCheckins,{ asOf, thresholds })` → ordered `{ kind, severity, message_key, context }[]`, each rule **suppressed** when its window lacks enough check-ins — FR-011; `energyHeatmap`/`sleepPerformanceScatter`/`overlapSeries` — scatter emits a point **only** for days with BOTH a check-in and a finished session, overlap gaps are `null` never `0`) + `services/recovery/{checkinView,alertsView,trendsView}.js` — all pure, test-first (Constitution V), no `@supabase` import. The clock is read **once** at the controller boundary (`isoDay(now())`) and passed to the guards/engine, never inside a pure fn (mirrors Phase 6/7/8).
- **Three deterministic, configurable, advisory alerts** (D-4), recomputed on read and **never persisted**: `high_stress` (stress ≥ `RECOVERY_STRESS_HIGH` on `RECOVERY_STRESS_HIGH_DAYS` consecutive check-in days), `reduce_volume` (avg sleep ≤ `RECOVERY_SLEEP_LOW_HOURS` AND avg energy ≤ `RECOVERY_ENERGY_LOW` over `RECOVERY_LOW_WINDOW_DAYS`), `full_rest` (≥ `RECOVERY_REST_SIGNALS` of {low sleep, low energy, high stress} in a day OR `sore_zones` count ≥ `RECOVERY_SORE_ZONES_REST`). **Endpoints** are a new `/api/v1/recovery` router (mounted with `{ daos, config }`, `recovery` dao wired in `app.js`): `GET /checkin` (saved values + `editable` + config-driven mood/zone option lists so the UI hardcodes neither), `PUT /checkin` (upsert), `GET /alerts`, `GET /trends?month=YYYY-MM`. Config keys: `RECOVERY_TREND_DAYS` (30), `RECOVERY_STRESS_HIGH` (7), `RECOVERY_STRESS_HIGH_DAYS` (3), `RECOVERY_SLEEP_LOW_HOURS` (6), `RECOVERY_ENERGY_LOW` (4), `RECOVERY_LOW_WINDOW_DAYS` (3), `RECOVERY_REST_SIGNALS` (3), `RECOVERY_SORE_ZONES_REST` (4), `RECOVERY_MOOD_OPTIONS` (`great,good,ok,low,bad`), `RECOVERY_SORE_ZONES` (`neck,…,calves`) — no `RECOVERY_LOCALE` (reuse `NUTRITION_LOCALE`).
- **Frontend reuses Phase 5 SVG `LineChart`** for the 30-day energy/stress/sleep overlay (energy+stress share the 0–10 axis, sleep hours on a secondary scale) + **two new pure `chartGeometry` helpers** `calendarMonth({ year, month, weekStartsOn })`/`scatterPoints({ points, xScale, yScale })` (unit-tested like `gaugeArc`/`donutSegments`, reuse `linearScale`) driving bespoke SVG `components/charts/{CalendarHeatmap,ScatterPlot}.jsx`; the body diagram is hand-rolled SVG. `/recovery` (30s check-in: star sleep quality, sliders, emoji `MoodPicker`, tap-to-toggle `BodyDiagram` sore zones, smart `RecoveryAlerts` panel with all-clear state) + `/recovery/trends` (monthly energy heatmap + sleep-vs-performance scatter + 30-day overlay) + "Récupération" nav. Live contract/integration tests probe for `recovery_log.sleep_quality` and **skip until the migration is applied**; integration tests snapshot-and-restore the current-week rows so they never destroy real recovery data.

## Phase 10 — Dashboard (added 2026-06-03)

- **Pure read-only composition — 0 migrations, 0 new tables, 0 new DAO, no new dependency**: the home screen is one composed endpoint `GET /api/v1/dashboard` whose controller (`dashboard.controller.js`, mounted with `{ daos, config }` after the other routers in `app.js`) fans out ~9 athlete-scoped reads with `Promise.all` and hands the plain data to pure presenters. Like Phase 5 load-tracking, it **never writes, never re-runs** the calculators/progression engine/`programGenerator`, and writes **no** `calculation_results`/`auditWriter` (FR-016/SC-010 — a GET changes no row counts). Every read is parameterised by `req.athleteId`; no athlete id is accepted from body/query (Constitution I).
- **Surfaces existing module outputs — only the streak + alerts are new logic** (research D-2): each tile maps to an existing reader/helper and is **not** re-derived — today's session from `weeklyPlan.dao.listSlotsWithExercises` + `sessionJournal/calendar.js#isoDayOfWeek`; its state (rest/start/resume/done) from `sessions.dao.findActiveForAthlete` + `sessions.dao.historyForEngine`; current phase + days remaining from `sessionJournal/currentPhase.js#{currentTrainingPhase,phaseForDate}` (+ `trainingPhases.dao` + profile `program_start_date`); weight vs start + 30-day series + goal from `bodyTracking/weightChartView.js#build`; yesterday's calories from `nutritionLogs.dao.listForDay` + `engine/nutritionMath.js#dayTotals` with the target from `nutrition/targets.js#resolveTargets`; the creatine streak from `engine/supplementStreaks.js#streakForSupplement` (+ `SUPPLEMENT_PRIMARY_SLUG`); ready-to-add-load from `progressionFlags.dao.findActiveForAthlete`; the daily quote from `quotes.dao#pickToday` (deterministic `dayIndex % count`, stable within a day). The 7-day strip reuses `supplements/week.js#{isoWeekStart,weekDays}`.
- **Pure boundary**: `services/engine/{sessionStreak,dashboardAlerts}.js` are the only genuinely new derived logic. `sessionStreak.js` — `consecutiveSessionStreak({ finishedDays, scheduleDays, asOf, programStart })` (consecutive **scheduled** training days completed in an unbroken run ending at the most recent **elapsed** scheduled day; rest days skipped, future days don't count, never counts before `programStart`, `0` when the latest elapsed scheduled day was missed) + `missedScheduledDays({ finishedDays, scheduleDays, asOf })` (consecutive elapsed scheduled training days at the tail with no completed session — drives `no_session`). `dashboardAlerts.js` — `aggregateAlerts(signals, { thresholds })`. View assembly lives in pure `services/dashboard/{todayCard,weekOverview,metricsView,dashboardView}.js` — all pure, test-first (Constitution V), no `@supabase` import; each tile presenter emits a `null`/empty/`has_data:false` cold-start shape so the whole payload renders for a brand-new athlete (FR-018/SC-009). The clock is read **once** at the controller boundary (`isoDay(now())` = server UTC day → `today`/`yesterday`/current ISO week/`programStart`/sparkline window) and injected as `asOf`; never inside a pure fn (mirrors Phase 6/7/8/9).
- **Exactly five fixed-priority alerts, capped at 3** (FR-010/FR-011, clarification D-3): `aggregateAlerts` considers exactly `{low_sleep_high_stress, no_session, calorie_deficit, ready_to_add_load, creatine_streak_broken}`, returns the active ones in the **fixed code order** `[low_sleep_high_stress, no_session, calorie_deficit, ready_to_add_load, creatine_streak_broken]`, top **3**. The order is **fixed in code**, not config; thresholds are config. `no_session` is **schedule-aware** (`missedScheduledDays ≥ DASHBOARD_NO_SESSION_DAYS`, default 2 — ordinary rest days never trigger it); `calorie_deficit` = yesterday `< DASHBOARD_CALORIE_DEFICIT_PCT × resolveTargets` (default 0.9); `low_sleep_high_stress` = latest recovery row sleep ≤ `RECOVERY_SLEEP_LOW_HOURS` AND stress ≥ `RECOVERY_STRESS_HIGH` (reuses Phase 9 thresholds, a direct latest-row check — **not** Phase 9's richer `evaluateAlerts`); `creatine_streak_broken.active` is true **only when the primary-supplement streak lapsed from a positive run to 0** — a brand-new athlete who never logged creatine (streak always 0) is **not** broken (SC-009).
- **Endpoints & config**: one additive path `GET /api/v1/dashboard` → `{ data: { today, week, metrics, sparkline, alerts, quote } }`. New config keys: `DASHBOARD_NO_SESSION_DAYS` (2), `DASHBOARD_CALORIE_DEFICIT_PCT` (0.9), `DASHBOARD_WEIGHT_SPARKLINE_DAYS` (30); reused (no new key): `RECOVERY_SLEEP_LOW_HOURS`, `RECOVERY_STRESS_HIGH`, `SUPPLEMENT_PRIMARY_SLUG`, and the resolved per-athlete nutrition target.
- **Frontend**: `DashboardHome.jsx` mounts at `/` and **replaces `ScaffoldHome`** as the home screen (nav "Accueil"); `ScaffoldHome` + its frontend test are **kept** (not deleted). It composes `components/dashboard/{TodaySessionCard,MetricCard×4,WeekStrip,AlertList,QuoteCard}.jsx` over a thin `lib/dashboardApi.js#getDashboard`; the today card's start/resume CTA links into the Phase 4 journal flow (the only action — the dashboard itself does no writes). The 30-day weight sparkline **reuses the Phase 5 SVG `LineChart`** (+ goal line) — no new chart geometry, no charting library; every other tile is plain Tailwind markup. Live contract/integration tests follow the Phase 5/7/8/9 live-gated pattern (skip when `.env`/Supabase is absent — **no migration probe needed; Phase 10 adds no schema**) and assert no row counts change across a GET (SC-010).

## Phase 11 — Statistics & Global Progress (added 2026-06-06)

- **Pure read-only composition + client-side PDF — 0 migrations, 0 tables, 0 new DAO, 1 new frontend dep (`jspdf`)**: the transformation-overview screen is two additive GETs under `/api/v1/statistics` (`statistics.controller.js` mounted with `{ daos, config }` after the dashboard router in `app.js`). Like Phase 5/10 it **never writes, never re-runs** the calculators/progression engine/`programGenerator`, writes **no** `calculation_results`/`auditWriter`, and contains **no AI** (recommendations are deterministic rule mappings). Every read is parameterised by `req.athleteId`; no id from body/query (Constitution I). The Constitution AI boundary holds through Phase 11.
- **Surfaces existing module outputs — only six pieces are genuinely new** (research D-2): each tab/metric maps to an existing reader and is not re-derived — weight curve+goal from `bodyTracking/weightChartView.js#build` (note it returns `point.kg`/`goalKg`, presenters remap to `weight_kg`/`goal_kg`), measurement series from `body_measurements` rows + `engine/measurementDeltas.js#MEASUREMENT_FIELDS`, calorie target from `nutrition/targets.js#resolveTargets` (×7 for the weekly target), calorie series from `engine/nutritionTrends.js`, weekly volume + attendance from `sessions.dao#dailyTrainingVolumes`, the exercise→muscle-group map from `loadTracking.controller.js#muscleGroupMaps`, ISO-week bucketing from `supplements/week.js#isoWeekStart`. Active `progression_flags` drive the report recommendations.
- **Six genuinely-new pure `services/engine/*` helpers (test-first, Constitution V)**: `statisticsMetrics.js` (D-7: total weight gained = latest − `athletes.starting_weight_kg`; total volume since start; completion rate = distinct finished days ÷ elapsed training-weekday occurrences in `[programStart, asOf]`; mean of per-ISO-week kcal totals), `exerciseImprovements.js` (D-4: `buildWorkingWeightSeries` + `rankByWorkingWeightGain` — top-N by **absolute working-weight gain in kg**, working weight = `one_rep_max_records.source_weight_kg`, ≥2 points, ties by exercise_id), `muscleGroupProgress.js` (D-5: `progressByGroup` = **progress % per group** now-vs-start, clamped ≥0, origin when insufficient), `attendanceHeatmap.js` (D-6: `gradeByVolume` — every day in `[from,to]`, level 0 = no session, 1..`STATISTICS_HEATMAP_LEVELS` quantile buckets of nonzero volumes), `stressWeightSeries.js` (D-9: `pairStressWeight` — a point only when BOTH a stress check-in and a body weight exist that day; `sufficient` below `STATISTICS_MIN_CORRELATION_POINTS`), `reportRecommendations.js` (D-11: `buildRecommendations` — fixed-order deterministic templated French lines from progression flags + calorie/recovery signals; `log_more_data` fallback; **no AI/free-form**).
- **Pure presenter boundary `services/statistics/*`** (`metricsView`, `bodyTab`, `strengthTab`, `attendanceTab`, `nutritionTab`, `recoveryTab`, `monthlyReport`, `statisticsView`) — assemble the contract view models from injected DAO data; each emits a cold-start/insufficient-data shape (FR-026) so a brand-new athlete renders fully. No `@supabase` import; the clock is read **once** at the controller (`asOf` = server UTC day) and injected; pure fns never read the clock/random/globals (mirrors Phase 6–10). `monthlyReport` is deliberately decoupled from `statisticsMetrics`/`exerciseImprovements` (the controller injects `lifetime`/`topProgressions`).
- **Endpoints & config**: `GET /api/v1/statistics` → `{ data: { metrics, body, strength, attendance, nutrition, recovery } }`; `GET /api/v1/statistics/report?month=YYYY-MM` → `{ data: { period, summary, lifetime, topProgressions, weightSeries, recommendations } }` (`month` optional, validated against `^\d{4}-(0[1-9]|1[0-2])$` → `400 VALIDATION_ERROR`; **defaults to the most-recently-completed calendar month**, Jan→prev-year Dec; `period.label` via `Intl.DateTimeFormat(NUTRITION_LOCALE, { …, timeZone: 'UTC' })`). New config keys: `STATISTICS_TOP_EXERCISES` (5), `STATISTICS_REPORT_TOP_PROGRESSIONS` (3), `STATISTICS_HEATMAP_LEVELS` (4), `STATISTICS_MIN_CORRELATION_POINTS` (3); reused (no new key): the resolved nutrition target, `RECOVERY_SLEEP_LOW_HOURS`/`RECOVERY_STRESS_HIGH` (report recovery flags), `NUTRITION_LOCALE` (strings + month labels).
- **Frontend**: `/statistics` screen (`pages/statistics/StatisticsHome.jsx`, nav "Statistiques") = 4 `StatMetricCard`s + a tab switcher over `tabs/{Body,Strength,Attendance,Nutrition,Recovery}Tab.jsx` + a `ReportButton`, fed by one `lib/statisticsApi.js#getStatistics` fetch. All five tabs **reuse the existing SVG charts** (`LineChart`/`BarChart`/`RadarChart`/`CalendarHeatmap`/`ScatterPlot`) — **no new chart geometry, no charting library**. The PDF is **client-side** (`lib/pdfReport.js`): fetch the report, render the weight `LineChart` to an off-DOM SVG, rasterize SVG→PNG via the browser canvas (`XMLSerializer`→`Image`→`canvas.toDataURL`), then `jspdf.addImage` + text — no `html2canvas`, no backend PDF infra; generating it persists nothing. Live contract/integration tests follow the Phase 5/7/8/9/10 live-gated pattern (no migration probe — Phase 11 adds no schema) and assert no row counts change across a GET (SC-011).
